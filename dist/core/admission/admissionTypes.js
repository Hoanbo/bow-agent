// src/core/admission/admissionTypes.ts
// BOWCON V4.0 — ZERO-TRUST ALWAYS-ON BRAIN CONNECTIVITY & SECURE INTERNET ADMISSION RUNTIME (MS-1.3.26)
//
// Canonical contracts, data models, error codes, and operational envelopes.
// STRICT INVARIANTS:
// - DISCOVERABLE != CONNECTABLE
// - CONNECTABLE != AUTHENTICATED
// - AUTHENTICATED != TRUSTED
// - TRUSTED != AUTHORIZED
// - AUTHORIZED != EXECUTED
// - KNOWING_BRAIN_ENDPOINT != ACCESS_TO_BRAIN
// - KNOWING_DEVICE_ID != POSSESSION_OF_DEVICE_KEY
// - POSSESSION_OF_DEVICE_KEY != EXECUTION_AUTHORITY
// - NETWORK_LOCATION != DEVICE_IDENTITY
// - IP_ADDRESS != DEVICE_IDENTITY
// - WI_FI_NETWORK != DEVICE_TRUST
// - PROTOCOL != DEVICE_TRUST
// - RECONNECT != RE-EXECUTE
// - ONE_BRAIN == ONE_AUTHORITATIVE_BRAIN
// - "Knowing where the Brain is does not mean having the right to enter it."
export const ADMISSION_PROTOCOL_VERSION = '4.0.0';
export const ALL_NETWORK_TYPES = Object.freeze([
    'WIFI',
    'CELLULAR_4G',
    'CELLULAR_5G',
    'WIRED',
    'HOTSPOT',
    'REMOTE_INTERNET',
    'UNKNOWN',
]);
export const ALL_ADMISSION_DECISION_TYPES = Object.freeze([
    'ADMIT',
    'REJECT',
    'CHALLENGE_REQUIRED',
    'REAUTHENTICATION_REQUIRED',
    'SESSION_RESUME_REQUIRED',
    'REVOKED',
    'EXPIRED',
    'SCOPE_MISMATCH',
    'CAPABILITY_REJECTED',
]);
export const ALL_ADMISSION_REJECTION_REASONS = Object.freeze([
    'UNTRUSTED_NETWORK_REJECTED',
    'INVALID_PROOF',
    'REPLAY_DETECTED',
    'DEVICE_REVOKED',
    'KEY_REVOKED',
    'TRUST_EXPIRED',
    'SCOPE_MISMATCH',
    'FORBIDDEN_CAPABILITY',
    'INVALID_ENDPOINT',
    'UNKNOWN_DEVICE',
    'DEVICE_NOT_TRUSTED',
    'CAPABILITY_REJECTED',
    'SESSION_TERMINATED',
    'TAMPER_DETECTED',
    'CROSS_TENANT_ACCESS',
    'RECONNECT_REEXECUTE_PREVENTED',
    'MALFORMED_REQUEST',
    'PROTOCOL_VERSION_MISMATCH',
]);
export const ALL_ADMISSION_SECURITY_EVENT_TYPES = Object.freeze([
    'ADMISSION_REQUESTED',
    'ADMISSION_ADMITTED',
    'ADMISSION_REJECTED',
    'CHALLENGE_ISSUED',
    'PROOF_VERIFIED',
    'REPLAY_ATTACK_BLOCKED',
    'REVOKED_DEVICE_BLOCKED',
    'EXPIRED_TRUST_BLOCKED',
    'SCOPE_MISMATCH_BLOCKED',
    'CAPABILITY_ESCALATION_BLOCKED',
    'ROAMING_MIGRATION_OBSERVED',
    'SESSION_RESUMED',
    'RECONNECT_REEXECUTE_PREVENTED',
    'UNAUTHORIZED_ENDPOINT_PROBE',
]);
